include ../mixins/sub-menu.pug

header.header(style='background: rgb(51, 51, 51);')
    .container
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href="/")
                        img(src="/images/favicon.svg", alt="Logo")
            .col-5
                form(
                    action="/search"
                    method="GET"
                ) 
                    div(class="form-group d-flex align-items-center mb-0")
                        input(
                            type="text"
                            placeholder="Nhập từ khóa..."
                            class="form-control"
                            name="keyword"
                            value=keyword
                        )
                        button(
                            type="submit"
                            class="btn btn-primary"
                            style="width: 115px"
                        ) Tìm kiếm
            .col-4
                .inner-menu
                    ul
                        li
                            a(href="/" style='color: white;') Trang chủ
                        li(class="sub-menu")
                            -const pathProducts = "/products"
                            a(href=pathProducts style='color: white;') Sản phẩm
                            if(layoutProductCategories)
                                +sub-menu(layoutProductCategories, pathProducts)
                        li
                            a(href="/cart" style='color: white;') Giỏ hàng (#{miniCart ? miniCart.products.length : 0})
                        if(user)
                            li
                                a(href="/user/profile" style='color: white;') #{user.fullName}
                            li
                                a(href="/chat" style='color: white;') Chat
                            li
                                a(href="/user/logout" style='color: white;') Đăng xuất
                        else
                            li
                                a(href="/user/login" style='color: white;') Đăng nhập